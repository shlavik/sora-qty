on:
  schedule:
    - cron: "* * * * *"
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: denoland/setup-deno@v1
      - run: ./run.sh
      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Qty grabbed
